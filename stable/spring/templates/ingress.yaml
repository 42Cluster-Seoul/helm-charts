{{- if .Values.service.enabled }}
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    alb.ingress.kubernetes.io/group.name: "42cluster"
    alb.ingress.kubernetes.io/group.order: "5"
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: "ip"
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}, {"HTTP":80}]'
    alb.ingress.kubernetes.io/backend-protocol: "HTTP"
    alb.ingress.kubernetes.io/certificate-arn: "arn:aws:acm:ap-northeast-2:616676703442:certificate/28c95859-9030-43d4-baf5-9daec0b99deb"
    alb.ingress.kubernetes.io/ssl-redirect: '443'
  name: {{ .Values.app.name }}
  namespace: {{ .Values.app.name }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: {{ .Values.ingress.path }}
        pathType: {{ .Values.ingress.pathType }}
        backend:
         service:
           name: {{ .Values.app.name }}
           port:
             number: {{ .Values.service.port }}
{{- end }}
{{- end }}